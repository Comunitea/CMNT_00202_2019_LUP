<?xml version="1.0"?>
<odoo>

   <!-- res.company inherit form view -->
   <record id="view_id" model="ir.ui.view">
        <field name="name">res.company.inherit.view.form</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Configuración costes">
                    <group>
                        <field name="ing_hours"/>
                        <field name="tech_hours"/>
                        <field name="help_hours"/>
                        <field name="km_cost"/>
                    </group>
                </page>
                <page string="Texto pedido venta lupéon">
                    <group>
                        <field name="lupeon_text" nolabel="1" mode="tree"/>
                    </group>
                </page>
            </notebook>
           
       </field>
   </record>

</odoo>